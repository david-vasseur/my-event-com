import LoginForm from '@/components/Form/LoginForm';
import Link from 'next/link';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default page;